import { ComposableElement, ComposableOptions } from '../types/composable'

export function initComposable(
  element: ComposableElement,
  options: ComposableOptions
): ComposableElement {
  throw Error('Not implemented yet!')
}
