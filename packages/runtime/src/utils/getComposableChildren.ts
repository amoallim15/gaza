import { ComposableElement } from '../types/composable'
import { ReferenceFragment } from '../types/fragment'

export function getComposableChildren(
  root: ReferenceFragment
): ComposableElement[] {
  throw Error('Not implemented yet.')
}
