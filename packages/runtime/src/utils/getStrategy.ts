import { ComposableElement } from '../types/composable'
import { ComposeStrategy } from '../types/strategy'

export function getStrategy(element: ComposableElement): ComposeStrategy {
  throw Error('Not implemented yet.')
}
